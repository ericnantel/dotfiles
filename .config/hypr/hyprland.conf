
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# SEE Hyprland/WorkspaceRules and WindowsRules
# SEE Pipshag/dotfiles_kitties (Github) to configure wofi and poweroff

# NOTE: Currently emacsclient is not opening in its workspace..
# TODO: Setup libreoffice, perhaps with emacs (then choose an icon for organization ?)
# NOTE: todo hyprlock, hyprcursor as well

#autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor = HDMI-A-1,preferred,auto,auto


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$browser = firefox
$gamecube = qemu #actually need to make a script for it..
$freedos = sh ~/ISOs/FD13-LiveCD/run_freedos.sh #for running dos games (however perhaps sdl is not working in hyprland..)
$winxp = sh ~/ISOs/WinXP-LiveCD/run_winxp.sh #for running old cd games
$menu = wofi --show drun


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = wezterm &
# exec-once = nm-applet &
exec-once = emacs --bg-daemon=gnu &
exec-once = waybar &
# exec-once = hyprpaper &
# exec-once = sh ~/.config/hypr/hyprwswp_d & # my script to switch wallpaper when switching workspace


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 5
    gaps_out = 20

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false 

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    drop_shadow = true
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1
        
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = ca
    kb_variant = fr
    kb_model =
    kb_options = ctrl:nocaps
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# bind = $mainMod, Q, exec, $terminal
bind = $mainMod, T, exec, wezterm # ghostty #kitty #gnome-terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, O, exec, emacsclient -r -s gnu
bind = $mainMod, E, exec, nautilus # dolphin
bind = $mainMod, F, exec, $browser
bind = $mainMod, D, exec, $freedos
bind = $mainMod, W, exec, $winxp
bind = $mainMod, V, togglefloating,
# bind = $mainMod, R, exec, $menu
bind = $mainMod, SPACE, exec, $menu
# bind = $mainMod, P, pseudo, # dwindle
# bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + [hjkl]
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [n(ext), p(revious)]
bind = $mainMod, n, workspace, +1
bind = $mainMod, p, workspace, -1

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Switch workspaces with mainMod + [numpad 0-9]
bind = $mainMod, KP_End, workspace, 1
bind = $mainMod, KP_Down, workspace, 2
bind = $mainMod, KP_Next, workspace, 3
bind = $mainMod, KP_Left, workspace, 4
bind = $mainMod, KP_Begin, workspace, 5
bind = $mainMod, KP_Right, workspace, 6
bind = $mainMod, KP_Home, workspace, 7
bind = $mainMod, KP_Up, workspace, 8
bind = $mainMod, KP_Prior, workspace, 9
bind = $mainMod, KP_Insert, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# My workspace monitor rules
workspace = 1, name: terminal_ws, monitor: HDMI-A-1, persistent: true, default: true
workspace = 2, name: browser_ws, monitor: HDMI-A-1, persistent: true, default: true
workspace = 3, name: emacs_ws, monitor: HDMI-A-1, persistent: true, default: true
workspace = 4, name: email_ws, monitor: HDMI-A-1, persistent: true, default: true
workspace = 5, name: explorer_ws, monitor: HDMI-A-1, persistent: true, default: true
workspace = 6, name: obs_ws, monitor: HDMI-A-1, persistent: true, default: true
workspace = 7, name: steamplay_ws, monitor: HDMI-A-1, persistent: true, default: true
workspace = 8, name: retrogame_ws, monitor: HDMI-A-1, persistent: true, default: true
# workspace = 9, name: extra1_ws, monitor: HDMI-A-1, persistent: true, default: true
# workspace = 10, name: extra2_ws, monitor: HDMI-A-1, persistent: true, default: true

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.

# My workspace window rules (https://github.com/ericnantel)
# NOTE: if you put silent before the comma, then it will open but not switch
windowrule = workspace 1, kitty
windowrule = workspace 1, ghostty
windowrule = workspace 1, wezterm
windowrulev2 = workspace 1, class:^(org.gnome.Terminal)$
windowrule = workspace 2, firefox
windowrule = workspace 3, emacsclient # TODO: Fix issue
windowrule = workspace 4, thunderbird
windowrulev2 = workspace 5, class:^(org.gnome.Nautilus)$
windowrule = workspace 6, obs
windowrule = workspace 6, vlc
windowrule = workspace 7, [Ss]team
windowrule = workspace 7, discord
windowrule = workspace 8, $gamecube
windowrule = workspace 8, $freedos
windowrule = workspace 8, $winxp
# TODO: Music, YouTube, News

# My opacity window rules (https://github.com/ericnantel)
windowrulev2 = opacity 0.90 0.90, class:^(firefox)$
windowrulev2 = opacity 0.90 0.90, class:^(Google-chrome)$
windowrulev2 = opacity 0.90 0.90, class:^(Brave-browser)$
windowrulev2 = opacity 0.80 0.80, class:^(discord)$

# My float window rules (https://github.com/ericnantel)
windowrulev2 = float, class:^(firefox)$, title:^(Picture-in-Picture)$
windowrulev2 = float, class:^(org.gnome.Nautilus)$
windowrulev2 = float, class:^(vlc)$
windowrulev2 = float, class:^([Ss]team)$
# windowrulev2 = float, class:^([Qq]emu)$
windowrulev2 = float, class:^(org.gnome.Calculator)$

# My center window rules (https://github.com/ericnantel)
windowrulev2 = center, class:^(vlc)$
# windowrulev2 = center, class:^([Qq]emu)$

# My keepaspectratio window rules (https://github.com/ericnantel)
windowrulev2 = keepaspectratio, class:^(firefox)$, title:^(Picture-in-Picture)$
windowrulev2 = keepaspectratio, class:^(vlc)$
# windowrulev2 = keepaspectratio, class:^([Qq]emu)$

# My immediate window rules (https://github.com/ericnantel)
windowrulev2 = immediate, title:.\*.exe #TODO: verify..
windowrulev2 = immediate, class:^(steam_app).*

